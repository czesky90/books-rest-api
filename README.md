# books-rest-api

An example REST API built on top of Flask.

### Endpoints (accordance with the guidelines)

| Method     | Endpoint          | 
|------------|-------------------|
| `GET`      | `/books`          |
| `GET`      | `/books/{bookid}` |
| `POST`     | `/db`             |